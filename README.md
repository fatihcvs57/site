# Sanayi Parçacı Online Sistem

## Çalıştırma

Node 18+

```bash
cp .env.example .env
npm install
npm run migrate
npm run seed
npm start
```

Uygulama: http://localhost:3000

İlk giriş: `admin@partshop.local / admin123`
